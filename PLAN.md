<!-- 작성: 2026-09-08 (3차).
     1차(2026-09-07): PC 스크립트 + 폰에서 텍스트 카드 편집 → "손으로 숫자 고치는 게 귀찮다"로 폐기.
     2차(2026-09-07): 단독 PWA → 사용자가 폰 Obsidian + Remotely Save(Dropbox) 볼트 "training"을 준비하고 Obsidian 플러그인을 선택해 폐기.
     3차: 기존 커뮤니티 플러그인 5종 조사(목표 자동 계산·A/B 자동 교대 없음) → 직접 만드는 플러그인으로 확정.
     작성 과정: 2차 계획의 엔진·화면·텍스트 형식을 승계해 초안 → Obsidian API/모바일·Remotely Save 동기화·헬스장 UX/과설계 관점 반박 검토 3명(이슈 27건) → 수정. -->

# 운동 일지 — Obsidian 플러그인 구현 계획

## 1. 목표와 요구사항

헬스장에 가는 날이 불규칙한 1인 사용자를 위해, 아이폰 Obsidian 안에서 버튼 몇 번으로 세트를 기록하고 다음 목표 무게를 규칙대로 미리 채워 주는 플러그인을 만든다. 기록은 `training` 볼트의 마크다운 파일 하나에 텍스트로 쌓이고, 이미 설치된 Remotely Save가 그 파일을 Dropbox로 올리므로 PC의 `C:\Users\USER\Dropbox\앱\remotely-save\training\` 폴더에 자동으로 도착한다. 서버·계정·수동 백업·플러그인 설정 파일이 없다. 폰에서 숫자를 타이핑하는 일은 없고, PC는 pandas로 읽거나 아주 가끔 텍스트를 손으로 고치는 역할만 맡는다.

핵심 요구 3가지:

1. **헬스장에서 버튼 몇 번** — 앱 아이콘을 누르면 아무 조작 없이 기록 화면이 떠 있고, 세트를 마치면 반복수 숫자 버튼 1번으로 기록이 끝난다. 무게를 바꾼 세트만 2~3탭. 화면에 직전 기록과 오늘 목표가 이미 떠 있고, 입력창이 없어 키보드가 뜨지 않는다. Obsidian이 백그라운드에서 죽어도 파일이 곧 상태이므로 그대로 복구된다.
2. **점진적 과부하** — 종목별 직전 1~2세션만 보고 다음 무게를 계산. 성공·유지·실패·디로드·장기 공백을 처리한다.
3. **변동적 일정** — 요일 개념 없음. "직전 세션이 A였나 B였나"만 본다. 2주 공백, 계획에 없던 종목, 중간에 끊긴 세션을 그대로 흡수한다.

## 2. 가정

| # | 가정 | 틀렸다면 바뀌는 것 |
|---|---|---|
| A1 | 아이폰, Mac 없음. Obsidian 모바일 1.5.7 이상(`vault.getFileByPath`가 있는 첫 버전)이며 커뮤니티 플러그인(비데스크톱 전용)을 실행한다. | 더 낮으면 `getAbstractFileByPath` + `instanceof TFile`로 바꾼다(2줄). |
| A2 | 폰의 Remotely Save는 PC와 같은 0.5.x 계열이고 Dropbox 연결이 끝났다(원격 폴더가 생겼으므로 확인됨). 기본 설정은 수동 동기화. | 버전이 달라도 4.6의 설정 항목 이름만 다를 뿐 계획은 그대로다. |
| A3 | GitHub 계정이 있고 공개 저장소 `training-log-program`에 코드만 올린다. 개인 기록은 저장소에 없다(볼트 안에만). | 비공개를 원하면 BRAT는 비공개 repo도 토큰으로 지원하지만, 대안인 4.1의 설정 폴더 동기화가 더 간단하다. |
| A4 | 초급~초중급. 세션마다 2.5/5 kg 올리는 이중 진행이 적절하다. | 중급 이상이면 `%% ex %%` 줄의 반복 범위·증가량만 바꾼다. 코드 변경 없음. |
| A5 | 상업 헬스장: 바벨(1.25 kg 원판 있음), 풀업바. 딥벨트 없음. 종목 6개(바벨 5 + 턱걸이)가 기본값. | 딥벨트 있으면 턱걸이 `incKg=2.5`. 머신은 `incKg`를 스택 단위로. |
| A6 | 주 2~4회, 종목당 3세트, 전신 A/B 두 템플릿. 하루 최대 1세션, 종목당 세션에 작업 무게 1개. | 주 4회 이상이면 템플릿 C(7절). |
| A7 | **폰이 유일한 기록 주체.** PC는 세션이 진행 중이 아닐 때, 폰에서 동기화 성공을 확인한 뒤에만 파일을 손으로 고친다. | 규칙을 어기면 Remotely Save가 수정 시각이 늦은 쪽으로 덮어쓴다. 플러그인이 헤더 시각 역행으로 감지해 빨간 띠를 띄우고, 복구는 Obsidian 파일 복구(7일)·Dropbox 버전 기록(30일). |
| A8 | 1인 개발·1인 사용. 대상은 iOS Obsidian 최신판 하나. `training` 볼트는 이 용도 전용이다. | 볼트를 다른 노트에도 쓰면 4.5의 "시작 시 항상 뷰 열기"를 조건부로 바꾼다. |

## 3. 사용자에게 확인할 결정 사항

각 항목의 기본값(추천)으로 진행하고, 답이 다르면 그때 바꾼다.

1. **데이터 파일** — **기본값: 볼트 루트의 `tlog.md` 하나.** 파일명은 ASCII로 고정한다(한글 파일명은 iOS↔Dropbox↔Windows 사이에서 NFC/NFD 정규화 차이로 파일이 둘로 갈리는 사례가 Remotely Save 이슈에 반복 보고됨). 내용·표시 이름의 한글은 문제없다. 종목 설정도 이 파일의 `%%` 주석 줄에 둔다. 플러그인 설정(`data.json`)은 만들지 않는다.
2. **종목 설정을 어디서 고치나** — **기본값: PC에서 `tlog.md`의 `%% ex %%` 줄을 텍스트로 편집**(메모장·VS Code·Obsidian 아무거나). 설정 화면은 MVP에 없다. 처음 한 번과 부상·종목 교체 때만 필요하며, 편집 후 폰에서 Obsidian을 열면 시작 시 동기화로 내려온다. **첫 파일은 반드시 폰이 만든다**(첫 세트 기록 시) — PC 편집은 그 파일이 Dropbox에 도착한 뒤부터. 폰에서 급하면 Obsidian 편집기로 그 줄을 직접 고쳐도 된다.
3. **배포 경로** — **기본값: 공개 GitHub 저장소의 Release + 폰의 BRAT 플러그인.** 대안은 Remotely Save의 실험 옵션 "설정 폴더 동기화"(4.1).
4. **동기화 시점** — **기본값: Remotely Save "시작 시 자동 실행 1초" + 플러그인이 "세션 끝내기"에서 동기화 명령 실행 + 앱이 다시 보일 때 미동기화 기록이 있으면 한 번 더 실행.** 주기 실행과 저장 시 동기화는 끈다(전자는 헬스장 통신 불량 시 오류 알림이 세션을 방해하고, 후자는 이 뷰에서 작동하지 않는다 — 4.6).
5. **뷰 여는 방법** — **기본값: 시작 시 항상 뷰를 연다**(볼트가 이 용도 전용이므로 조건 없음). 예비 경로는 설정 → 모바일 → "Quick Action 구성"에 `운동 일지: 열기`를 배정(아래로 당기기 제스처 1번, 키보드 없음)과 리본 아이콘. 모바일 툴바는 쓰지 않는다 — 편집기에 키보드가 올라와 있을 때만 보이는 편집 툴바라 이 뷰에서는 존재하지 않는다.
6. **PC에서 `training` 폴더를 Obsidian 볼트로 여는가** — **기본값: 열지 않는다.** 개발은 별도 로컬 볼트 `C:\Users\USER\dev\training-log-program\tlog-vault`, 조회·수정은 텍스트 편집기와 pandas. 열어도 해가 없지만(`.obsidian`은 Remotely Save가 기본으로 동기화하지 않음) 두 볼트가 헷갈린다.
7. **종목 목록** — **기본값: squat, bench, row / deadlift, ohp, pullup.** `id`는 기록에 영구히 남으므로 바꾸지 않는다. 표시 이름(한글)은 자유.
8. **시작 무게(`startKg`)** — **기본값: 비움.** 첫 목표가 하한(바벨 20, 맨몸 0)으로 뜨고 기록 화면의 ±(길게 누르면 반복)로 맞춘다.
9. **경험 수준·턱걸이·템플릿 수** — **기본값: 5–8회 이중 진행, 턱걸이 `incKg=0, repHigh=99`, 전신 A/B 2개.**
10. **정정 범위** — **기본값: 오늘 세션은 화면에서 세트 행 탭 → 숫자 교체(2탭) 또는 지우기. 과거 세션은 PC에서 파일을 고친다.**

## 4. 핵심 설계

### 4.1 플랫폼·기술 스택·배포

**선택: TypeScript + esbuild로 만든 Obsidian 커뮤니티 플러그인 `tlog`. 상태는 전부 볼트 안 파일 하나.**

- 의존성은 Obsidian 샘플 플러그인과 같은 개발용 3개(`obsidian`, `esbuild`, `typescript`)뿐. 런타임 의존성 0. 샘플의 `builtin-modules`·`@types/node`는 쓰지 않는다.
- 규칙·파서는 DOM과 Obsidian API를 전혀 모르는 `src/logic.ts`에 순수 함수로 두고, Node 24가 타입을 그대로 벗겨 실행하므로 컴파일 없이 `node --test test/logic.test.ts`로 검증한다. 이 파일에서는 enum·namespace·생성자 매개변수 프로퍼티를 쓰지 않고, import는 `"../src/logic.ts"`처럼 확장자를 붙인다(`tsconfig`에 `allowImportingTsExtensions`, `noEmit`).
- 플러그인 설정 탭·`data.json`·`loadData/saveData`는 쓰지 않는다. `.obsidian`은 동기화되지 않으므로 거기에 둔 값은 PC에 도착하지 않는다.

**파일 배치**

```
C:\Users\USER\dev\training-log-program\        # git 저장소 (GitHub: training-log-program)
  manifest.json          # {"id":"tlog","name":"운동 일지","version":"0.1.0",
                         #  "minAppVersion":"1.5.7","isDesktopOnly":false}
  package.json           # scripts: dev / build / test / check
  tsconfig.json  esbuild.config.mjs
  styles.css             # .tlog-view 아래로만 범위 지정
  src\main.ts            # class TlogPlugin: registerView, addCommand, addRibbonIcon, onLayoutReady, visibilitychange
  src\view.ts            # class TlogView extends ItemView (홈·기록 두 모드)
  src\store.ts           # 볼트 I/O: readDoc(vault.read), updateDoc(직렬화된 vault.process, 없으면 create)
  src\logic.ts           # 순수 함수: localDate, daysBetween, roundDown, judge, historyOf,
                         #   nextTarget, nextSession, todayPlan, toLogText, parseLogText, defaultDoc
  scripts\check.ts       # npm run check: Dropbox 사본을 parseLogText로 검사, 오류면 줄 번호 출력
  test\logic.test.ts     # node --test
  README.md  PLAN.md
C:\Users\USER\dev\training-log-program\tlog-vault\                  # PC 개발용 로컬 볼트 (Dropbox 밖)
  tlog.md
  .obsidian\plugins\tlog\{main.js, manifest.json, styles.css}   ← esbuild 출력 대상
C:\Users\USER\Dropbox\앱\remotely-save\training\   # 폰 볼트의 원격 복사본. PC는 읽기·가끔 수정
  tlog.md
```

`versions.json`은 커뮤니티 스토어용이라 만들지 않는다.

**esbuild 설정** — `platform: "browser"`, `format: "cjs"`, `target: "es2018"`, `external`은 `obsidian`, `electron`, `@codemirror/*`, `@lezer/*`만. Node 내장 모듈을 external에 두지 않으므로 실수로 `fs`/`path`를 import하면 아이폰에서 죽는 대신 빌드에서 잡힌다.

**개발 루프(PC)** — `esbuild.config.mjs`의 `outfile`을 환경변수로 나눈다: `npm run dev`는 `--watch`로 `tlog-vault\.obsidian\plugins\tlog\main.js`에 직접 쓰고, `npm run build`는 production 빌드(인라인 소스맵 없음)로 저장소 루트 `main.js`에 쓴다(Release용). `manifest.json`·`styles.css`는 처음 한 번 dev 볼트에 복사한다. PC Obsidian에 `tlog-vault`를 열고 커뮤니티 플러그인에서 `tlog`를 켠 뒤, 수정할 때마다 명령 팔레트 "Reload app without saving"(Ctrl+P)으로 다시 읽는다. 창을 400px 폭으로 줄이면 폰 레이아웃을 흉내낼 수 있다.

**폰 설치 절차(BRAT, 한 번만)**

1. PC: `manifest.json`의 `version`을 올려 커밋 → `git push` → `git tag 0.1.0`(태그 = version 문자 그대로, `v` 접두사 없음) → `git push --tags` → 그 태그로 GitHub Release 생성(draft 아님), 자산으로 `npm run build`가 만든 `main.js`, `manifest.json`, `styles.css` 세 파일을 그대로 첨부(폴더·zip 아님). BRAT는 저장소 HEAD의 `manifest.json`에서 version을 읽은 뒤 `releases/download/<version>/`을 받으므로 세 가지(푸시된 manifest, 같은 이름의 태그, 공개된 Release)가 모두 맞아야 한다.
2. 폰 Obsidian, `training` 볼트: 설정 → 커뮤니티 플러그인 → 제한 모드 끄기 → 탐색 → "BRAT" 설치·활성화(스토어에 있으므로 일반 설치).
3. 설정 → BRAT → "Add Beta plugin" → `https://github.com/<계정>/training-log-program` 입력 → Add. BRAT가 최신 Release를 내려받아 `.obsidian/plugins/tlog/`에 둔다.
4. 설정 → 커뮤니티 플러그인 → `운동 일지` 켜기. 이후 Obsidian을 열 때마다 뷰가 자동으로 뜬다.
5. (예비) 설정 → 모바일 → "Quick Action 구성" → 명령 `운동 일지: 열기` 지정. 어느 화면에서든 아래로 당기기 한 번으로 열린다.
6. 갱신: PC에서 1단계를 반복(버전 올려 커밋·푸시·태그·Release) → 폰 BRAT "Check for updates"(또는 BRAT 설정의 시작 시 자동 갱신).

**대안 1개: Remotely Save "설정 폴더 동기화"(experimental `syncConfigDir`)** — GitHub 없이 PC 볼트의 `.obsidian/plugins/tlog/`를 Dropbox로 올려 폰이 받게 한다. 단, PC가 `training` 원격 폴더를 볼트로 열고 Remotely Save까지 설치해야 하며(결정 6과 충돌), `.obsidian` 전체(다른 플러그인 설정·workspace)가 양방향으로 섞인다. GitHub 계정이 없거나 BRAT가 끝내 실패할 때만. (iOS 파일 앱은 `.`으로 시작하는 폴더를 보여 주지 않으므로 세 파일을 손으로 복사하는 길은 없다.)

### 4.2 데이터 파일

**파일: `tlog.md` (볼트 루트). 전체 상태의 단일 원본. 메모리 캐시 없음.**

```
%% tlog v1 2026-09-07T20:15:33+09:00 %%
%% ex squat A 3 5 8 5 60 스쿼트 %%
%% ex bench A 3 5 8 2.5 40 벤치 %%
%% ex row A 3 5 8 2.5 40 바벨로우 %%
%% ex deadlift B 3 5 8 5 80 데드리프트 %%
%% ex ohp B 3 5 8 2.5 30 오버헤드 프레스 %%
%% ex pullup B 3 5 99 0 0 턱걸이 %%
2026-09-07 session A
2026-09-07 squat 60x8 60x8 60x8
2026-09-07 bench 40x8 40x7 40x6
2026-09-09 session B
2026-09-09 pullup 0x6 0x6 0x5
```

- 첫 줄은 형식 버전과 **마지막으로 폰이 쓴 시각**(초 단위, 로컬 오프셋). 이 시각의 용도는 하나 — 동기화가 폰 기록을 덮어썼는지 감지(아래 "동기화 충돌 대응" 3). `%%` 줄은 **`%%`로 시작하고 `%%`로 끝나야 하며**, 끝 `%%`를 뗀 뒤 `%% ex id template sets repLow repHigh incKg startKg 이름`에서 `startKg`는 `-`로 비울 수 있고, 8번째 이후 토큰은 전부 이름으로 join한다(공백 포함 가능). 닫는 `%%`가 없으면 거부한다(Obsidian 읽기 모드에서 다음 `%%`까지 본문이 통째로 숨겨지므로). 본문 줄은 `날짜 id kgxreps ...`, 날짜 블록은 `session` 줄로 시작하고 날짜 오름차순.
- **세 곳에서 읽힌다.** Obsidian 읽기 모드에서는 `%%` 줄이 숨겨지고 본문만 한 줄씩 보인다(단일 줄바꿈이 그대로 표시되는 기본 설정). 플러그인은 `parseLogText`로 읽는다. PC는 pandas가 `comment="%"`로 주석 줄을 통째로 건너뛴다.
- **활성 세션 = 파일의 오늘 날짜 블록.** 별도 상태 파일이 없다. 오늘 블록이 있고 어떤 종목이든 세트가 `sets` 미만이면 "진행 중", 날짜가 지나면 그대로 기록이다. "시작하기"를 눌러 첫 세트를 찍기 전까지는 파일에 아무것도 쓰지 않는다 — **세트 0개 세션은 파일에 존재하지 않는다**(잘못 누른 시작이 A/B 순서를 바꾸지 않는다). **현재 종목**은 뷰 메모리에만 두고, 뷰를 다시 열면 `todayPlan`이 **오늘 블록의 마지막 entry(가장 최근에 세트를 찍은 종목)** 가 `sets` 미만이면 그것, 아니면 목록에서 그 다음의 미완료 종목으로 되돌린다. 건너뛴 종목이 앞에 있어도 하던 종목으로 돌아온다.
- 타입(`logic.ts`): `Exercise {id, name, template, sets, repLow, repHigh, incKg, startKg: number|null}`, `SetRec {kg, reps}`, `Session {date, template, entries: {id, sets: SetRec[]}[]}`, `Doc {writtenAt, exercises, sessions}`. 진행 상태(실패 연속, 직전 템플릿, 공백)는 저장하지 않고 매번 재계산한다. `defaultDoc()`은 기본 종목 6개(startKg `-`)·세션 0개.
- **날짜**: `localDate(d = new Date())`가 `getFullYear/getMonth+1/getDate`로 조립. `toISOString`은 UTC라 아침 운동이 전날로 저장되므로 쓰지 않는다. 공백 일수는 `Date.UTC`로 차이를 구한다.

**쓰기 전략 — 탭마다 전체 파일 재작성, `vault.process` 한 번, 플러그인 안에서 직렬화.**

```ts
// store.ts
let queue: Promise<void> = Promise.resolve();
function updateDoc(app: App, mutate: (doc: Doc) => void): Promise<void> {
  queue = queue.then(async () => {
    const file = app.vault.getFileByPath(LOG_PATH);
    if (!file) {                                   // 첫 세트: 여기서만 파일을 만든다
      const doc = defaultDoc(); mutate(doc);
      await app.vault.create(LOG_PATH, toLogText(doc));
      return;
    }
    await app.vault.process(file, (text) => {
      const doc = parseLogText(text);              // 실패하면 throw → 파일은 그대로, Notice에 줄 번호
      mutate(doc);
      return toLogText(doc);                       // 첫 줄 시각 갱신
    });
  });
  return queue;
}
```

- 파일은 세트당 30바이트, 1년치 100 KB 미만이라 파싱+재작성이 5 ms 아래다. append는 정정·지우기에 어차피 못 쓰므로 경로를 하나로 둔다.
- `vault.process`의 원자성은 한 호출 안의 읽기-변경-쓰기에만 해당하고 같은 파일에 대한 동시 호출이 직렬화된다는 보장은 없다. 그래서 프로미스 체인 한 줄로 **플러그인 쓰기끼리** 직렬화하고, 쓰기가 끝날 때까지 기록 버튼을 비활성화한다. 연타해도 세트가 사라지지 않는다. Remotely Save는 vault API가 아니라 `adapter.writeBinary`로 내려쓰므로 이 직렬화 밖에 있지만, 그 경쟁은 시작 후 1초 부근(시작 시 동기화)으로 한정되고 mtime이 늦은 폰 쪽이 이긴다. 뷰는 이 창을 피해 시작 2초 뒤에 연다(4.5).
- **파일 생성은 첫 세트의 `updateDoc` 안에서만.** 플러그인 로드나 뷰 열기에서 파일을 만들지 않는다 — 시작 시 동기화보다 먼저 기본 파일을 만들면 원격에 이미 있던 파일을 mtime 경쟁으로 덮어쓸 수 있다. 파일이 없으면 뷰는 `defaultDoc()`으로 홈을 그리고 "파일 없음 — 첫 세트에 생성"을 표시한다.
- **파싱 실패 = 쓰기 거부.** `parseLogText`는 첫 줄이 `%% tlog v1 ...`이 아니면(빈 파일·잘린 파일 포함) 1행으로, `%% ex %%`에 없는 `id`, 닫는 `%%` 없는 주석 줄, `session` 줄 없는 블록, 중복·역순 날짜, `squat 100x` 같은 깨진 세트를 **줄 번호와 함께 전체 거부**한다. 플러그인은 `Notice("tlog.md 12행: ...")`를 띄우고 홈에 빨간 띠 + **"파일 열기" 버튼**(`workspace.getLeaf(true).openFile(file)`, 폰 편집기에서 그 줄을 고치는 유일한 복구 수단)을 남긴다. 잠긴 동안은 **"동기화" 버튼도 비활성**(손상본 업로드 방지). 조용히 덮어쓰는 경우는 없다. `\r\n`은 파서가 `\r`을 벗기고, BOM은 첫 문자에서 벗기며, 출력은 항상 `\n`·UTF-8(BOM 없음).

**동기화 충돌 대응** — Remotely Save는 파일 단위로 수정 시각이 늦은 쪽을 남긴다. 병합은 없다. 그래서 규칙을 "충돌이 생길 수 없게" 두고, 생기면 **시끄럽게** 알린다.

1. 세션 중 쓰는 쪽은 폰뿐(A7). 도중에 동기화가 돌아도 올라가는 것은 폰의 최신본이라 무해하다.
2. PC 손 수정은 **폰에서 동기화 성공을 본 뒤**에 한다 — "세션 끝내기"의 Remotely Save 성공 알림을 봤거나, 집에서 Obsidian을 한 번 열어 시작 시 동기화(= 업로드)가 끝난 뒤. PC 사본의 헤더 시각으로는 폰에 미동기화 기록이 있는지 알 수 없다(PC가 보는 것은 마지막으로 도착한 쓰기일 뿐). 의심되면 Dropbox 웹의 수정 시각을 본다.
3. **덮어쓰기 감지(비교 1줄).** 플러그인은 마지막으로 읽거나 쓴 헤더 시각을 메모리에 들고, `modify` 이벤트로 파싱한 새 헤더 시각이 그보다 **과거**면 "동기화가 폰 기록을 덮어씀(20:15 → 어제) — 설정 → 파일 복구에서 되돌리기" 빨간 띠 + 쓰기 잠금. PC 손 수정은 헤더를 바꾸지 않으므로 규칙 2를 지킨 편집은 헤더가 같아 경보가 없고, 폰에 안 올라간 기록이 있었을 때만 울린다.
4. PC에서 고친 뒤에는 폰에서 Obsidian을 한 번 열면 시작 시 동기화가 받아온다. 플러그인은 `modify` 이벤트로 `tlog.md` 변경을 감지해 다시 그리므로(4.5), 동기화가 끝나면 화면이 새 값으로 바뀐다.
5. 그래도 덮어쓰면 Obsidian 코어 "파일 복구"(폰에도 스냅샷을 남김; 설정을 간격 1분·보관 7일 이상으로)와 Dropbox 웹의 버전 기록(30일)으로 되돌린다. 복원된 파일은 mtime이 최신이라 다음 동기화가 그것을 올린다. 플러그인은 백업 파일을 만들지 않는다.

**PC에서 읽기**

```python
import pandas as pd
df = pd.read_csv(r"C:\Users\USER\Dropbox\앱\remotely-save\training\tlog.md", sep=r"\s+", engine="python",
                 comment="%", header=None, names=["date","id","s1","s2","s3","s4","s5","s6"], encoding="utf-8")
df = df[df.id != "session"]   # 이후 s1..s6을 "kgxreps"로 split
```

`sep=r"\s+"`라 후행 공백에 흔들리지 않는다. 세트가 6개를 넘으면 `names`를 늘린다(README). 손 수정 후에는 `npm run check`(logic.ts의 `parseLogText`를 이 사본에 실행, 오류면 줄 번호 출력)를 돌려 폰에서 거부당하는 일을 막는다.

### 4.3 점진적 과부하 엔진

> **2026-09-08 폐기.** 사용자 피드백: "다음에 +5 kg를 정해 놓으면 안 된다. 점진적 과부하는 주·월 단위로 점차 늘려 가는 것이다." 아래 규칙 엔진은 구현에서 제거했다. 현재 동작은 9절 변경 이력 참조: 지난번 값 미리 채움 + 정보성 힌트 + 주간·월간 추세.


`logic.ts`의 순수 함수 `nextTarget(ex, hist, lastSessionDate, today) -> {kg, reason}`. `hist`는 그 종목의 세션 목록(최신 우선, `{date, sets}`)이며 `historyOf(sessions, id)`가 만든다. 화면에는 `kg × repLow`가 목표로 표시되고 `reason`이 한 줄로 찍힌다.

**정의**

- `last` = 이 종목의 가장 최근 세션, `W` = 그 세션의 최대 무게, `judged` = `W`에서 한 **처음 `n`세트**(더 가벼운 세트는 워밍업, `n`세트 이후는 무시).
- `lo, hi, n, inc` = 종목 설정값. `roundDown(x, inc)` = `inc` 단위 내림 후 하한(바벨 20, 맨몸 0) 적용; `inc=0`이면 `x` 그대로. 내림은 다음 목표 계산에만 쓰고, 기록 무게는 어떤 값(62.5, 82.5)이든 그대로 받는다.
- **성공** = `judged`가 `n`세트이고 모두 `reps >= hi`.
- **실패** = `judged` 중 어느 세트라도 `reps < lo`. 마지막 세트 1회 부족도 실패다 — 의도적으로 엄격하며 M4 조정 대상.
- **세트 부족은 실패가 아니다.** `n`보다 적게 했으면 성공만 못 할 뿐, 있는 세트로 유지/실패를 판정한다.
- **공백**은 종목이 아니라 **마지막 세션(어느 템플릿이든)** 기준(`lastSessionDate`)이다. 주 1회 A/B 교대에서 종목별 간격은 항상 14일이므로 종목 기준이면 매번 감량된다.

**규칙 (위에서부터 첫 번째 해당 항목 적용)**

```
1 첫 세션   기록 없음                                   → startKg ?? 하한   "첫 세션" / "시작 무게 미설정"
2 공백      today - lastSessionDate ≥ 14일              → roundDown(W×0.9)  "14일+ 공백 -10%"
3 성공                                                 → W+inc             "+inc: 전 세트 hi회 달성"
4 디로드    직전 2세션이 연속으로 같은 W에서 실패          → roundDown(W×0.9)  "디로드 -10%: 2연속 실패"
5 유지      그 외                                       → W                 "유지" / "실패 1회: 재시도"
```

- 목표 반복수는 항상 `lo`로 표시하고, 옆에 "`hi`회면 +`inc`"를 함께 보여 준다.
- 규칙 4는 직전 두 세션만 본다. 실패 → 유지 → 실패는 디로드가 아니다. 하한 20에서 디로드가 걸리면 20을 유지한다.
- 정체 판정, 유지 시 `+1회` 목표, 2회째 디로드 경고, 28일 공백 단계, RPE, 주기화는 MVP에서 제외(7절).

**검증 표 (bench, lo 5 / hi 8 / inc 2.5 / n 3)** — 각 행이 `test/logic.test.ts`의 `test()` 하나다.

| 직전 세션 | 다음 목표 | reason |
|---|---|---|
| (없음, startKg 40) | 40 × 5 | 첫 세션 |
| 40x8 40x8 40x8 | 42.5 × 5 | +2.5: 전 세트 8회 달성 |
| 42.5x7 42.5x6 42.5x5 | 42.5 × 5 | 유지 |
| 42.5x6 42.5x5 42.5x4 | 42.5 × 5 | 실패 1회: 재시도 |
| 42.5x5 42.5x4 42.5x4 (2연속 실패) | 37.5 × 5 | 디로드 -10% (38.25→37.5) |
| 실패 → 42.5x6 42.5x6 42.5x5 → 실패 | 42.5 × 5 | 실패 1회: 재시도 (연속 아님) |
| 50x6 50x6 50x6, 마지막 세션이 20일 전 | 45 × 5 | 14일+ 공백 -10% |
| 42.5x6 42.5x6 (2세트만) | 42.5 × 5 | 유지 (세트 부족은 실패 아님) |
| 42.5x8 42.5x8 42.5x8 42.5x3 | 45 × 5 | 처음 3세트만 판정 |
| 20x4 20x4 20x4, 2연속 실패 | 20 × 5 | 디로드 -10% (하한 20) |
| squat 60x8×3 성공 후 2연속 실패 | 55 × 5 | 내림 단위 = inc 5 (58.5→55) |

추가 테스트: 7일 간격 A,B,A,B 로그에서 어떤 종목에도 공백 감량이 없음; `pullup`(inc 0, hi 99)은 `0x10 0x10 0x10` 후에도 `0 × 5`; `startKg` 없는 종목은 `20 × 5` "시작 무게 미설정"; `localDate(new Date(2026, 8, 7, 0, 30))`이 `"2026-09-07"`; `toLogText → parseLogText` 왕복이 `{writtenAt, exercises, sessions}`와 깊은 동일(한글 이름·`startKg` 비움·`\r\n` 입력·BOM 포함).

### 4.4 변동적 일정 처리

달력이 없다. 순수 함수 `nextSession(exercises, sessions, template = null) -> {template, exercises[]}`.

1. **직전 템플릿** = `sessions` 마지막 원소의 `template`. 세션이 없으면 직전 없음.
2. **다음 템플릿** = 직전이 아닌 쪽. 빈 기록 → A. 2일 뒤에 오든 12일 뒤에 오든 같다.
3. **장기 공백**은 순서에 영향 없음. 4.3의 공백 규칙이 무게만 낮춘다. 홈에 "14일+ 공백: 목표 -10%" 띠를 띄운다.
4. **재량** — 홈의 "대신 A 하기" 버튼 1번(오늘 블록이 있으면 숨김 — 템플릿은 첫 세트에서 확정된다). 기록 화면의 "종목 추가"는 **`%% ex %%`에 있으나 오늘 목록에 없는 종목**을 이름 + `nextTarget` 결과와 함께 나열하고, 하나를 탭하면 현재 종목 바로 뒤에 삽입되어 그리로 이동한다(2탭). 새 종목 정의는 파일 편집으로만.
5. **세션 중단·재개 — 파일이 곧 상태.** `todayPlan(doc, today)`가 오늘 블록을 찾는다. 없으면 홈. 있으면 오늘 목록 = 블록의 종목 순서 + 그 템플릿에서 아직 안 한 종목, 현재 종목 = 마지막 entry가 미완료면 그것, 아니면 그 다음 미완료 종목(4.2). **오늘 블록이 있으면 뷰는 기록 모드로 직행**한다(홈 생략). Obsidian이 죽었다 살아나도, 뷰를 닫았다 열어도 같은 계산이므로 0탭 복구다. 날짜가 지난 블록은 손댈 것 없이 그대로 기록이다(자동 저장 로직 없음). 세트 부족은 실패가 아니므로 중단된 세션도 불이익이 없다.
6. **같은 날 두 번** — "세션 끝내기"로 홈에 온 뒤 오늘 블록이 있으면 홈의 주 버튼은 **"오늘 B 이어하기 (5세트)"**. 하루 1세션 불변식은 파일 문법(한 날짜 = 한 블록)이 지킨다.
7. **세션 끝내기**는 파일을 바꾸지 않는다. 동기화 명령을 실행하고 홈으로 돌아갈 뿐이다. 오탭해도 잃는 것이 없다.

### 4.5 화면/흐름

뷰 하나(`TlogView`, 뷰 타입 `"tlog-view"`)에 홈·기록 두 모드. 입력창이 없어 키보드가 뜨지 않는다. 버튼 높이 44px 이상, 숫자 28px 이상. 자주 누르는 것은 아래쪽.

**뷰 여는 경로** — (a) `onLayoutReady` 2초 뒤 **조건 없이** `open()`(볼트가 이 용도 전용이므로; 2초는 시작 시 동기화의 쓰기 창을 피하기 위함), (b) Quick Action 제스처 또는 리본 아이콘 `dumbbell`로 명령 `tlog:open`, (c) 워크스페이스 복원 — 지난번 이 뷰가 앞에 있었으면 Obsidian이 그대로 복원한다. `open()`은 `workspace.getLeavesOfType("tlog-view")`가 있으면 그 리프를 `revealLeaf`, 없으면 `getLeaf(false).setViewState({type:"tlog-view", active:true})`. 이미 열려 있을 때 다시 불러도 같은 리프를 앞으로 가져올 뿐이라 무해하다. 오늘 블록 유무는 홈/기록 모드만 결정한다.

**뷰 생명주기** — `onOpen`은 DOM만 만들고(`contentEl.addClass("tlog-view")`) 파일 접근은 전부 `this.app.workspace.onLayoutReady(() => this.load())` 안에서 한다(복원된 리프의 `onOpen`은 볼트 인덱스가 끝나기 전에 불릴 수 있어 `getFileByPath`가 null을 준다; 이미 준비됐으면 즉시 실행됨). `load()` = `readDoc`(`vault.read`, `cachedRead` 아님) → 파싱 → 렌더. `onClose`는 아무것도 저장하지 않는다(이미 파일에 있다). 재읽기 트리거: `vault.on("modify")`(경로 일치 + 150 ms 디바운스 — 자기 자신의 `vault.process`도 이 이벤트를 내므로), `workspace.on("active-leaf-change")`로 뷰가 다시 앞에 올 때. 기록 모드에서는 목표·직전 기록만 갱신하고 현재 종목·스테퍼 값은 유지한다.

**① 홈** — `nextSession`·`nextTarget`을 계산해 그린다.

```
┌──────────────────────────────┐
│ ⚠ tlog.md 12행: 깨진 세트 [파일 열기] │  ← 파싱 오류·덮어쓰기 감지 때만. 있으면 버튼 비활성
│ 다음: B  (직전 A · 9/7, 2일 전)│
│                              │
│ 데드리프트  85 kg × 5  3세트   │
│   +5: 전 세트 8회 달성        │
│ 오버헤드    30 kg × 5  3세트   │
│   유지                        │
│ 턱걸이       0 kg × 5  3세트   │
│   첫 세션                     │
│                              │
│ [        B 시작하기        ]  │  ← 오늘 블록이 있으면 "오늘 B 이어하기 (5세트)"
│ [ 대신 A 하기 ]  [ 동기화 ]   │  ← 오늘 블록이 있으면 "대신 A" 숨김
│      tlog 0.1.0 · 마지막 기록 9/7 20:15 │  ← 헤더 시각. 동기화 시각은 Remotely Save 상태바
└──────────────────────────────┘
```

**② 기록** — 종목 하나씩.

```
┌──────────────────────────────┐
│ 데드리프트  1/3               │
│ 지난번 9/2: 80×5 80×5 80×5    │
│ 목표 85 × 5   (8이면 +5)      │
│                              │
│   [−]     85 kg     [+]      │  ← ±2.5 고정, 길게 누르면 반복
│                              │
│ [3][4][5][6][7][8][9][10]    │  ← 44px씩, 5·8은 색 구분. 탭 = 세트 기록
│                              │
│ 1세트 85×5   2세트 85×4       │  ← 행 탭 → 그 세트 수정 모드
│ [ 이 세트 지우기 ]             │
│ [ ‹ 이전 ]        [ 다음 › ]  │
│ [ 종목 추가 ]  [ 세션 끝내기 ] │
└──────────────────────────────┘
```

- **반복수 버튼 줄** = `max(1, lo-2)` … `lo+5`(lo 5면 3~10). `click`만 쓴다(스크롤 중 오탭 방지). 숫자를 누르면 `updateDoc`으로 현재 무게의 세트가 오늘 블록에 붙는다(첫 세트면 `session` 줄과 함께 블록 생성, 파일이 없으면 파일 생성). 쓰기가 끝날 때까지 줄 전체를 비활성화한다. **범위 안의 어떤 결과든 항상 1탭**, 완료 버튼 없음. `lo`·`hi` 버튼은 색으로 구분.
- **무게 ±**는 `STEP = 2.5` 고정(`incKg`는 계산 전용), 하한 0(`Math.max(0, kg - STEP)` — 턱걸이에서 음수 방지). `click` 핸들러 없이 `pointerdown`에서 `el.setPointerCapture(e.pointerId)` 후 즉시 1스텝, 300 ms 뒤부터 초당 5회 반복. 정지 조건: `pointerup`, `pointercancel`, `lostpointercapture`, `pointerleave`, `document.visibilitychange`(hidden), `onClose`, 그리고 반복 40스텝 상한. 버튼은 `touch-action: none`(`manipulation`은 팬을 허용해 길게 누르는 중 손가락이 조금만 움직여도 스크롤로 넘어가 반복이 끊긴다). 종목 화면을 열 때 초기값 = 오늘 이 종목의 마지막 세트 kg, 없으면 목표 kg. 즉 뷰 메모리가 날아가도 파일에서 복구된다.
- **자동 이동 없음.** `n`세트를 채우면 큰 버튼 "다음: 오버헤드 30×5 ›"(마지막이면 "세션 끝내기")가 나타나고 반복수 줄은 남아 4세트째도 1탭.
- **정정**: 세트 행 탭 → 강조 + 반복수 줄이 수정 모드 → 숫자 하나로 교체(2탭). "이 세트 지우기"는 선택된 세트, 없으면 마지막 세트. `logSet(reps, index = null)`, `deleteSet(index)`. 지워서 종목의 세트가 0개가 되면 그 줄을, 블록이 비면 `session` 줄까지 지운다.
- **세션 끝내기**: 1탭. `(app as any).commands.executeCommandById("remotely-save:start-sync")`를 부르고 홈으로 간다. 명령이 없으면(false 반환) "동기화 명령 없음 — 리본에서 수동 실행" Notice.
- 세트당 탭 수: 같은 무게 1탭, 무게 변경 2~3탭(± 1~2 + 숫자 1), 정정 2탭, 종목 이동 1탭.

**모바일 제약** — `styles.css`에서 `.tlog-view button { touch-action: manipulation; -webkit-touch-callout: none; -webkit-user-select: none; user-select: none; -webkit-tap-highlight-color: transparent; }`(± 버튼만 `touch-action: none`)로 더블탭 확대, 길게 누를 때의 콜아웃과 텍스트 선택 돋보기를 막는다. 상태바·홈 인디케이터 여백은 Obsidian 워크스페이스가 처리하므로 뷰는 `contentEl` 안에서 세로 스크롤만 한다(하단 버튼을 `position: fixed`로 두지 않는다). DOM 이벤트는 `this.registerDomEvent`로 등록해 `onClose`에서 자동 해제한다. Quick Action(아래로 당기기)이 뷰 상단 스크롤에서 걸려도 같은 뷰를 다시 앞으로 가져올 뿐이라 무해하다.

### 4.6 동기화·PC 연동

**Remotely Save 설정(폰, 한 번)**

| 항목 | 값 | 이유 |
|---|---|---|
| Run once on startup automatically | 1초 | PC 손 수정을 다음 세션 전에 받고, 지난 세션의 미동기화분을 올린다. |
| Run automatically every N | 끔 | 세션 중 통신 불량 알림을 피한다. |
| Sync on save (experimental) | 끔 | 이 뷰에서는 작동하지 않는다 — `workspace.getActiveFile()`의 mtime만 보는데 ItemView가 활성이면 null이다. |
| Sync config dir | 끔 | 기본값. 결정 3의 대안을 택할 때만 켠다. |
| **Protect modify percentage(변경 비율 보호)** | **100** | 기본 50%면 파일이 1~2개뿐인 이 볼트에서 `tlog.md`가 바뀐 모든 동기화가 "changing files >= 50%"로 중단된다. 100이면 통과(사실상 해제). |
| Enable mobile status bar | 켬 | 진짜 마지막 동기화 시각을 화면에 보인다(플러그인은 알 수 없음). |

**코어 "파일 복구"(폰, 한 번)** — 설정 → 파일 복구: 스냅샷 간격 1분, 보관 7일 이상. 기본 5분이면 세션 마지막 몇 세트가 빠진다.

**세션 끝 동기화** — 4.5의 "세션 끝내기"가 `remotely-save:start-sync`를 실행한다. 이 id는 PC에 있는 v0.5.25 코드에서 `id:"start-sync"`로 확인한 후보이며, M2b에서 `executeCommandById`의 반환값으로 검증한다. 홈의 "동기화" 버튼도 같은 명령이다.

**복귀 시 재동기화** — iOS는 앱을 며칠씩 서스펜드로 두므로 앱 전환기로 다시 여는 것은 `onLayoutReady`도 "Run once on startup"도 다시 타지 않는다. 헬스장에서 실패한 동기화가 무기한 남지 않도록, 플러그인은 `document`의 `visibilitychange`(visible)에서 `마지막 기록 시각 > 마지막 동기화 명령 시각`이면 `start-sync`를 한 번 더 부른다(등록 1개, 시각 변수 2개; 이미 올라간 상태면 Remotely Save가 동일 판정으로 끝내 무해).

**PC에서 읽기** — Dropbox 데스크톱이 `training\tlog.md`를 몇 초 안에 내려 준다. 4.2의 pandas 세 줄과 `npm run check`. PC 쪽 코드는 그 외에 없다.

**손 수정 규칙(README)** — (1) 세션 중이 아닐 때, (2) 폰에서 동기화 성공을 확인한 뒤(세션 끝 알림 또는 집에서 Obsidian 한 번 열기), (3) 파일 이름·위치는 절대 바꾸지 않고 원본 위치에서 내용만 편집, (4) 헤더 줄은 손대지 않고 문법을 지켜서(형식은 파일 자체가 예시), 저장 후 `npm run check` 통과, (5) 그 다음 폰에서 Obsidian을 한 번 열어 받는다. 파서가 거부하면 폰 홈에 줄 번호와 "파일 열기" 버튼이 뜬다. Dropbox가 `tlog (충돌하는 사본).md`를 만들면 폰 홈이 "충돌 사본 발견: PC에서 병합 후 삭제" 띠를 띄운다(`vault.getFiles()`에서 `tlog`로 시작하는 다른 `.md` 필터 1줄).

## 5. MVP 범위

**포함**

- 플러그인 파일 일체(`manifest.json`, `main.ts`, `view.ts`, `store.ts`, `logic.ts`, `styles.css`, esbuild 설정), `scripts/check.ts`, GitHub Release, BRAT 설치 절차 README
- `tlog.md` 문법·직렬화된 `updateDoc(vault.process)`·첫 세트에서 파일 생성·파싱 거부(줄 번호 + 파일 열기 버튼)·헤더 역행 감지·충돌 사본 띠·`modify`(디바운스)·`active-leaf-change` 재렌더
- 홈·기록 두 모드, 반복수 버튼 줄(1탭), 무게 ±2.5 + 포인터 캡처 길게 누르기, 세트 행 탭 수정·지우기, 종목 추가(파일 종목 목록에서 선택), 대신 A 하기, `n`세트 후 "다음 종목" 버튼, 오늘 블록 있으면 기록 모드 직행·홈의 이어하기
- `localDate`, `nextTarget`(첫 세션·공백·성공·디로드·유지·내림·하한), `nextSession`, `todayPlan`, `defaultDoc`
- 공백 띠, 파싱 오류 띠, 버전·마지막 기록 시각 표시
- 뷰 열기 3경로(시작 시 항상·Quick Action/리본 명령·워크스페이스 복원), 세션 끝·홈 버튼·복귀 시의 동기화 명령 실행
- `node --test`: 4.3 표 11행 + 추가 테스트 + 4.4 규칙별 1개 + `todayPlan` 4건(블록 없음/진행 중/전부 완료/건너뛴 종목이 앞에 있어도 현재 종목은 마지막 entry) + 파서 거부 6건 + 텍스트 왕복
- README: 설치(BRAT 6단계), Remotely Save 설정 6개 + 파일 복구 설정, 최초 설정 순서(플러그인 켜기 → 첫 세트로 파일 생성 → 동기화 1회 → 그 다음 PC 편집), 종목 설정 편집법, 손 수정 규칙 5개, pandas·`npm run check`, 제약(하루 1세션·작업 무게 1개·`id` 불변·세트 6개 초과 시 `names`)

**명시적 제외** — 설정 탭·`data.json`, 백업·복원 UI, 휴식 타이머, 차트·히스토리 화면, 과거 세션 편집 UI, 정체 판정, RPE, 주기화, 메모·체중, 1RM, 워밍업 계산, 데스크톱 전용 기능, 다국어, 데일리 노트 연동, 플러그인 자체 백업 파일, 커뮤니티 스토어 등록.

## 6. 단계별 마일스톤

**M1 — 규칙 엔진·파서 (1일, 하루 안에, PC 터미널만)**
- 산출물: `git init`, `package.json`(`"test": "node --test test/logic.test.ts"`, `"check": "node scripts/check.ts"`), `tsconfig.json`, `src/logic.ts`, `scripts/check.ts`, `test/logic.test.ts`.
- 검증: `npm test` 전부 통과 — 4.3 표 11행, `startKg` 비움 → 하한, `localDate` 00:30, 빈 기록 → A, 마지막 A → B, `template` 강제, 7일 간격 무감량, pullup `0 × 5`, `todayPlan` 4건, 왕복 동일성(한글 이름·`-`·`\r\n`·BOM), 거부 6건(`squat 100x`·`session` 없는 블록·모르는 `id`·중복/역순 날짜·헤더 없음/빈 파일·닫는 `%%` 없는 주석 줄)을 줄 번호와 함께 거부. `npm run check`가 정상 파일에 OK, 망가뜨린 파일에 줄 번호를 출력.

**M2a — 플러그인 뼈대 + 홈·기록 (2일째, PC Obsidian `tlog-vault`)**
- 산출물: `manifest.json`, `esbuild.config.mjs`(dev outfile → 볼트, external 최소), `main.ts`, `store.ts`, `view.ts`, `styles.css`. 리본·명령 `tlog:open`.
- 검증: 플러그인을 켜면 파일 없이 홈이 "파일 없음 — 첫 세트에 생성"과 A 제안 → 시작 → 첫 숫자 탭에 `tlog.md`가 헤더·기본 종목 6줄·`session A`·세트 1개로 생성됨(파일을 옆 탭에 열어 확인) → 세트 9개를 숫자 버튼만으로 기록 → 숫자 버튼 5회 빠른 연타 → 파일에 5세트 → ±로 62.5 기록, 길게 누르면 반복, 40스텝에서 멈춤, 0에서 − 눌러도 0 → 행 탭 → 숫자로 교체 → 3세트 뒤 "다음: …" 버튼, 자동 이동 없음 → 끝내기 → 홈이 B 제안, 무게가 규칙대로. 뷰를 닫고 다시 열면 기록 모드 직행, 같은 종목·세트·무게; 벤치를 건너뛰고 로우를 하다 닫아도 로우로 복귀. 세트를 전부 지우면 블록이 사라지고 홈이 다시 A. 창 폭 400px에서 가로 스크롤 없음.

**M2b — 파일 이벤트·오류·동기화 명령 (3일째, PC Obsidian)**
- 산출물: `modify` 디바운스 재렌더, `active-leaf-change` 재읽기, 파싱 오류 띠 + 파일 열기 버튼, 헤더 역행 감지 띠, 충돌 사본 띠, `onLayoutReady` 2초 뒤 항상 열기, "종목 추가", "대신 A 하기"(오늘 블록 있으면 숨김), 끝내기·복귀 시 `executeCommandById`. PC `tlog-vault`에 Remotely Save를 임시 설치(연결 안 해도 명령은 등록됨).
- 검증: 파일을 편집기에서 고치면 홈 숫자가 바뀜(탭마다 홈이 깜빡이지 않음). `60x8`을 `60x`로 망가뜨리면 띠에 줄 번호가 뜨고 버튼·동기화가 잠기며 숫자를 눌러도 파일이 안 바뀜; 파일 열기 버튼으로 편집기가 열림. 파일을 비우면 1행 거부. 헤더 시각을 과거로 고쳐 저장하면 "덮어씀" 띠 + 잠금. `tlog (충돌하는 사본).md`를 만들면 띠. "Reload app" 직후(오늘 블록 유무 모두) 뷰가 자동으로 열리고 파일을 정상 로드, 진행 중이면 기록 모드로 감. `executeCommandById("remotely-save:start-sync")`가 `true`를 반환(아니면 실제 id를 찾아 상수 수정). 세트 없이 시작만 누르고 닫으면 파일 변화 없음.

**M3 — 아이폰 설치·동기화 왕복 (4일째, 실제 아이폰 + PC)**
- 산출물: `npm run build`, 커밋·푸시·태그 `0.1.0`·Release(자산 3개), README 설치 절차, 폰 Remotely Save 설정 6개 + 파일 복구 설정.
- 검증: BRAT로 설치 → **세트 1개 기록 → 세션 끝내기 → Remotely Save 알림이 오류가 아닌 성공**(변경 비율 보호 확인) → 앱을 완전히 종료하고 아이콘 탭 → 아무 조작 없이 기록 화면이 떠 있음 → Quick Action 제스처로도 열림 → 세트 몇 개 기록(반복수 버튼 엄지로 한 손 조작, 빠른 두 번 탭에 확대 없음, 길게 눌러도 콜아웃·돋보기 없음, 키보드 안 뜸) → ±를 누른 채 앱 전환 후 복귀해도 무게가 멈춰 있음 → 앱 전환기에서 Obsidian을 밀어 올려 종료 → 다시 열면 뷰와 종목·세트·무게가 그대로 → 세션 끝내기 → 1분 안에 PC `training\tlog.md`가 도착하고 내용이 4.2 형식 → pandas로 세션 수 일치. PC에서 한 세트를 고쳐 저장(`npm run check` 통과) → 폰 Obsidian 완전 종료 후 열기 → 시작 시 동기화 뒤 뷰를 닫지 않아도 홈 숫자가 바뀐 값 반영. 비행기 모드에서 끝내기 → 오류 알림만, 파일은 온전 → 해제 후 홈 화면에 갔다가 앱 전환기로 복귀 → 자동 동기화 성공. 세트 기록 후 설정 → 파일 복구로 이전 스냅샷 복원 → 세트가 되돌아오고 다음 동기화가 그것을 올림.

**M4 — 실사용 2주 (아이폰 + PC)**
- 산출물: 버그 수정만. 불편한 점 목록.
- 검증: 기간의 모든 세션이 PC 파일에 있고 Dropbox 버전 기록에 덮어쓰기 흔적이 없다. PC에서 `logic.ts`를 `node`로 돌려 "세트 kg == 그 시점 `nextTarget` kg"인 세트 비율 90% 이상. 동기화를 잊어 PC 도착이 하루 이상 늦은 세션이 2회 이상이면 종목의 `n`세트 완료(다음 종목 버튼 표시) 시점마다 `start-sync`를 호출하도록 바꾼다(코드 1줄, 상수 1개). 임계값(-10%, 14일, 2연속, 실패 정의)은 여기서 처음 조정한다.

## 7. 이후 확장 후보

M4 불편 목록이 요구할 때만, 한 번에 하나씩: 종목 완료마다 동기화 호출; 반복수 버튼에 붙는 휴식 타이머(90초); 홈에 "지난번 못 한 종목 [추가]" 1탭 제안; 뷰 안 종목별 최근 5세션 표; 정체 판정·2회째 디로드 경고·유지 시 `+1회`; 28일 공백 단계(-20%), 템플릿 C, 종목별 1.25 kg 증가량; PC `analyze.py`(Epley 1RM 추세, matplotlib); Dataview로 `tlog.md`를 데일리 노트에 표시(PC 코드 없이).

## 8. 리스크

- **덮어쓰기 충돌** — Remotely Save는 병합 없이 수정 시각으로 승자를 정한다. 폰에 안 올라간 세션이 있는데 PC가 파일을 고치면 폰이 다음에 켜질 때 원격이 폰 기록을 덮는다. 대응: A7 규칙(폰에서 동기화 성공 확인 후 편집), 세션 끝·복귀 시·시작 시 동기화로 미동기화 창을 짧게, 헤더 시각 역행 감지로 시끄럽게 알림 + 잠금, 파일 복구(1분·7일)·Dropbox 버전 기록(30일)으로 되돌림.
- **세션 중 PC 수정본 도착** — 시작 시 동기화가 세션 시작 직후 끝나면 PC 수정본이 내려온다. 플러그인 쓰기는 파일의 최신 내용 위에 세트를 얹으므로 잃는 것은 없고, `modify` 재렌더가 목표만 갱신하고 현재 종목·스테퍼는 유지한다. 외부 쓰기와 `vault.process`의 경쟁은 시작 1초 부근으로 한정되며 뷰는 2초 뒤에 열린다.
- **파일 손상·문법 오류** — 손 수정 실수, 편집기의 BOM·`\r\n`, 잘못된 `id`, iOS 종료로 잘린 파일(0바이트 포함). 대응: 파서가 헤더 없음까지 포함해 줄 번호와 함께 전체 거부하고 쓰기·동기화를 잠근다(손상본이 Dropbox에 올라가 PC 사본을 덮는 일이 없다); `\r`·BOM 제거; 폰에서는 "파일 열기" 버튼으로 그 줄을 고치거나 파일 복구로 되돌린다; PC에서는 `npm run check`. 조용한 덮어쓰기는 설계상 없다.
- **BRAT 설치 실패** — Release 자산 이름이 정확히 `main.js`/`manifest.json`/`styles.css`가 아니거나, `manifest.version`과 태그가 다르거나(`v` 접두사 포함), manifest가 푸시되지 않았거나, Release가 draft이거나, `isDesktopOnly`가 true면 폰에서 안 보인다. 대응: M3 체크리스트로 재시도; 끝내 안 되면 대안은 설정 폴더 동기화(4.1)뿐이다.
- **첫 파일 생성과 동기화의 경쟁** — 플러그인이 시작 직후 기본 파일을 만들면 원격의 진짜 파일을 덮을 수 있다. 대응: 파일 생성은 첫 세트의 `updateDoc` 안에서만; README 최초 설정 순서 고정.
- **iOS가 Obsidian을 죽임** — 뷰 메모리(현재 종목, 스테퍼 값, 선택 행)만 사라진다. 전부 파일에서 다시 계산되며 마지막 entry가 현재 종목, 마지막 세트 kg이 스테퍼 초기값이므로 82.5가 85로 돌아가지 않고 건너뛴 종목으로 되돌아가지도 않는다.
- **비공개 API** — `app.commands.executeCommandById`는 타입에 없다. `any` 캐스트 한 줄이며, 없으면 Notice로 수동 동기화를 안내한다. 커뮤니티 스토어에 올리지 않으므로 심사 문제 없음.
- **길게 누르기 오동작** — 손가락이 버튼 밖으로 나가거나, 전화·알림·앱 전환으로 WebView가 멈췄다 돌아오거나, 시스템 제스처가 터치를 가로채면 `pointerup`이 오지 않아 무게가 폭주할 수 있다. 대응: 포인터 캡처, 정지 조건 6개(`visibilitychange`·`onClose` 포함), 40스텝 상한, `touch-action: none`, `-webkit-touch-callout`·`-webkit-user-select`. M3에서 실기기 확인.
- **동기화 명령 id 변경** — Remotely Save 갱신으로 id가 바뀔 수 있다. 대응: 상수 하나, 실패 시 Notice + 리본.
- **헬스장 통신 불량** — 세션 끝 동기화 실패. 파일은 폰에 온전하고, 앱이 다시 보일 때 미동기화 기록이 있으면 플러그인이 다시 올리며, 콜드 스타트면 시작 시 동기화가 올린다. PC 도착이 늦을 뿐 데이터 손실은 없다.
- **자정을 넘긴 세션** — 자정 뒤 찍은 세트는 새 날짜 블록으로 들어간다(하루 1세션 문법). 드물고 세트 부족은 불이익이 없으므로 감수한다.
- **엄격한 실패 정의** — 디로드가 잦으면 M4에서 "총반복 < n×lo"로 완화. 상수 3개(-10%, 14일, 2연속)는 `logic.ts` 상단에만 있다.
- **범위 확장 유혹** — 설정 탭·타이머·차트는 각각 싸 보인다. M4(실사용 2주 전 추가 금지)가 유일한 방어선이다.

## 9. 변경 이력

- **2026-09-08 (M2 실사용 피드백)**: 사용자 요청으로 "기록 화면에 입력창 없음" 원칙을 완화했다. (1) 무게 숫자를 누르면 직접 입력, (2) 반복수 버튼 줄 옆에 "다른 횟수" 입력, (3) "종목 추가"에서 새 종목 이름을 입력해 만들면 `%% ex %%` 줄로 저장(id = 이름의 공백을 `_`로, 한글 허용; 파서의 id 규칙을 "공백·% 불가, session 예약"으로 완화). 목표 안내 문구를 "(8이면 +5)"에서 "3세트 모두 8회 이상이면 다음엔 +5 kg"로 바꿨다. 개발용 볼트는 프로젝트 안 `tlog-vault\`(git 제외).
- **2026-09-08 (종목 삭제)**: 기록 화면 하단 "이 종목 삭제"(2탭 확정). 지난 기록이 없으면 `%% ex %%` 줄을 지우고, 있으면 파서가 기록을 거부하지 않도록 템플릿을 `-`(숨김)로 바꿔 목록에서만 뺀다. 숨긴 종목은 "종목 추가 → 숨긴 종목"에서 복원(오늘 템플릿으로). 오늘 세트가 있는 종목은 삭제 불가(세트 먼저 삭제). `templatesOf`는 `-`를 제외한다.
- **2026-09-08 (엔진 폐기 → 기록·추세 중심)**: 자동 목표 계산(성공 시 +inc, 디로드, 공백 감량)과 종목의 반복 범위·증가량·시작 무게 필드를 모두 제거. 기록 화면은 지난번 값(최고 무게, 마지막 횟수)을 미리 채우고, 정보성 힌트(`GAP_DAYS`=14일 만의 운동, 같은 최고 무게 `SAME_STREAK`=3세션 연속, 직전 두 세션 최고 무게 변화)만 보여 준다. "기록 보기"에 날짜별 기록·주간 최고(월~일)·월간 최고 추가. 파일 형식 v2: `%% ex id 템플릿 세트수 이름 %%`, 세션 줄에 시작 시각 `날짜 session A 18:32`(첫 세트 시각). v1 파일은 읽기만 지원. 홈과 기록 화면에 날짜·요일·시각을 표시. 맨몸 체크박스 제거(무게 0으로 기록). 단위 테스트 25개, 스모크 23단계.
- **2026-09-08 (세트 수 계획 제거)**: 사용자 요청("세트 수도 내가 정해야지"). 종목의 `sets` 필드와 "n/3세트"·n세트 뒤 자동 등장하던 다음 버튼을 없앴다. 화면은 "오늘 2세트"처럼 찍은 수만 세고, 다음 종목은 항상 보이는 "다음: 벤치 ›"로 넘어간다. `todayPlan`의 현재 종목 = 블록의 마지막 entry. 파일 형식 v3: `%% ex id 템플릿 이름 %%`. v1·v2 읽기 유지.
- **2026-09-08 (화면 단순화)**: 사용자 피드백("버튼이 너무 많고 기록하는 법을 모르겠다"). 기록 화면을 무게 −/+, 횟수 −/+, 큰 "기록" 버튼 셋으로 줄였다(지난번 값 미리 채움, 숫자를 누르면 직접 입력). 반복수 버튼 8개·"다른 횟수" 입력·세트 행 선택/교체·별도 삭제 버튼을 없애고 "마지막 지우기" 하나로 정정한다. 이동은 "‹ 이전 / 다음: X ›" 한 줄, 마지막 종목에서는 "세션 끝내기 ✓". 기록 보기·종목 추가·중간에 끝내기·종목 삭제는 "더보기 ⋯" 안으로. 홈은 큰 "시작하기" 하나와 글자 링크 두 개. `repsRange` 제거. 단위 테스트 24개, 스모크 24단계.
- **2026-09-08 (오늘 기록 표시 버그)**: 세션을 끝낸 뒤 홈이 종목마다 "기록 없음"을 보이고 "기록 보기"가 오늘 세트를 빼고 보여 줘서 저장이 안 된 것처럼 보였다(과거 기록만 쓰는 도우미를 표시에도 썼음). 홈 제목을 "오늘 A · 18:32 시작 · 9세트 저장됨"으로, 종목 줄은 오늘 세트가 있으면 "오늘: …"로, 기록 보기는 오늘 포함으로 고쳤다. PC처럼 Remotely Save가 없는 볼트에서 뜨던 알림 문구를 "기록은 저장됐습니다. 동기화는 건너뜀"으로 바꿨다.
- **2026-09-08 (오늘 기록 수정·삭제)**: 사용자 요청. "오늘 n세트" 줄의 세트가 버튼이 되어, 누르면 값이 무게·횟수 칸에 올라오고 큰 버튼이 "n세트 수정"으로 바뀌며 "n세트 삭제"·"취소" 링크가 나온다. "마지막 지우기"는 이 방식에 흡수. 홈에서 오늘 기록이 있으면 종목 줄을 눌러 그 종목의 기록 화면으로 바로 간다(세션을 끝낸 뒤 수정용).
